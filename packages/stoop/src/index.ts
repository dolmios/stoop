/**
 * Main entry point for Stoop
 * Re-exports createStoop and all types
 */

export { createStoop } from "./create-stoop";

export type {
  CSS,
  ComponentProps,
  DefaultTheme,
  StoopConfig,
  StoopInstance,
  Theme,
  ThemeScale,
  UtilityFunction,
  VariantProps,
  StyledComponent,
  StyledFunction,
} from "./types";
