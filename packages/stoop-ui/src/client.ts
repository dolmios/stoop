"use client";

/**
 * Client-only exports from stoop-ui.
 * These are advanced APIs that should only be used in client components.
 *
 * Import from "stoop-ui/client" to use these:
 * ```tsx
 * "use client";
 * import { styled, keyframes, globalCss } from "stoop-ui/client";
 * ```
 */

export { styled, keyframes, globalCss } from "./stoop.theme";
