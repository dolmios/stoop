# css

<Icon name="Info" size={24} style={{ display: "inline", verticalAlign: "middle", marginRight: "8px" }} /> Create CSS classes with theme tokens

## Usage

```typescript
import { css } from "./theme";

const buttonClass = css({
  padding: "$medium $large",
  backgroundColor: "$primary",
  color: "$background",
  "&:hover": {
    opacity: 0.9,
  },
});
```

## With Media Queries

```typescript
const responsiveClass = css({
  padding: "$small",
  mobile: {
    padding: "$medium",
  },
  desktop: {
    padding: "$large",
  },
});
```

## Dynamic Classes

```typescript
const dynamicClass = css({
  backgroundColor: "$background",
  color: "$text",
});
```

## Returns

Returns a string containing the generated CSS class name. Use this class name in your JSX:

```typescript
const buttonClass = css({ padding: "$medium" });

<button className={buttonClass}>Click me</button>
```
